from setuptools import find_packages
from setuptools import setup

setup(
    name='roboy_info_system_msg',
    version='0.0.0',
    packages=find_packages(
        include=('roboy_info_system_msg', 'roboy_info_system_msg.*')),
)
