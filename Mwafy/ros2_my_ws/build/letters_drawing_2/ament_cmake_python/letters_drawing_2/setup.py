from setuptools import find_packages
from setuptools import setup

setup(
    name='letters_drawing_2',
    version='0.0.0',
    packages=find_packages(
        include=('letters_drawing_2', 'letters_drawing_2.*')),
)
